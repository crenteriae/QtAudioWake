QSlider {
    min-height: 20px;
}

QGroupBox {
    font-weight: bold;
    border: 1px solid palette(light);
    border-radius: 5px;
    margin-top: 10px;
}

QGroupBox::title {
    subcontrol-origin: margin;
    left: 10px;
    padding: 0 5px;
}

#infoLabel {
    color: palette(midlight);
    font-size: 11px;
}
